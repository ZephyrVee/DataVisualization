<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.datavisualization">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DataVisualization">
        <activity android:name=".KelolaDataActivity"></activity>
        <activity android:name=".visualisasi.MainPage" />
        <activity android:name=".VisualisasiActivity" />
        <activity android:name=".VisualisasiTestActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RadarChartActivity" />
        <activity android:name=".PieChartActivity" />
        <activity android:name=".LineChartActivity" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>